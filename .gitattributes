# Normalize all text files to LF in the repo and working tree.
# The repo was authored on Linux; this keeps Windows checkouts consistent
# and stops CRLF churn in diffs.
* text=auto eol=lf
*.mp3 binary
*.png binary
*.jpg binary
