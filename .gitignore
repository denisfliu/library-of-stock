# Cached API responses
cache/

# Full qbreader mirror (seeded from official backups; rebuild via lib/mirror/)
/mirror/

# CSV reference data
csvs/

# Python
__pycache__/
*.pyc

# Local only
serve.sh
queue/*.lock
queue/.batch.lock

# Personal study notes (math/CS curriculum)
study/

# Generated site artifacts — rebuilt by ./build.sh; CI builds and deploys these
/index.html
output/**/*.html
output/guides_data.js
dev/score_clues_review.html
dev/changelog_data.json
dev/crossrefs_data.json
dev/stats_data.json
