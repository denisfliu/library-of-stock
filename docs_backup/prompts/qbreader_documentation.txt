

The API documentation for QB Reader.

For Python developers, a Python API wrapper providing both synchronous and asynchronous interfaces is available on our GitHub, as well as PyPI under the name qbreader. Documentation for the library can be found on Read the Docs.

Base URL: https://www.qbreader.org/api

New rooms are created on the server when a WebSocket connection is established.

GET requests should always have parameters passed through the URI, and POST requests should have parameters passed through the body.

The API is rate-limited to 20 requests per second.
Endpoints
Database

    GET /query
    Query the database.
    GET /random-bonus
    Get random bonuses from the database.
    GET /random-tossup
    Get random tossups from the database.
    GET /packet
    Get a specific packet from a set.
    GET /num-packets
    Get the number of packets in a set.
    GET /set-list
    Get a list of all sets in the database.
    GET /bonus
    Get a single bonus by its ID.
    GET /tossup
    Get a single tossup by its ID.
    GET /frequency-list
    Get the most frequent tossup and bonus answers for the given subcategory.

Multiplayer

    GET /multiplayer/room-list
    Get all public rooms.

Question Stats

    GET /question-stats/bonus
    Get aggregated QBReader play statistics for a given bonus.
    GET /question-stats/tossup
    Get aggregated QBReader play statistics for a given tossup.

Other

    GET /check-answer
    Judge an answer to be correct, incorrect, or prompt, given an answerline.
    GET /random-name
    Get a random adjective-noun pair that can be used as a name.
    POST /report-question
    Report a question.

